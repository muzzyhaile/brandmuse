# Copy to .env.local and fill these
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Together AI (server-side only — DO NOT expose secrets via Vite)
# Store your Together API key only in server/edge environments (e.g., Supabase Edge Functions)
# Do not prefix with VITE_.
TOGETHER_API_KEY=

# Optional: Public base URL for client to point to your server/edge proxy (not the key)
VITE_TOGETHER_PROXY_URL=

# Example server-side cURL (for reference only; run from backend/edge)
# curl -X POST "https://api.together.xyz/v1/chat/completions" \
#   -H "Authorization: Bearer $TOGETHER_API_KEY" \
#   -H "Content-Type: application/json" \
#   -d '{
#     "model": "openai/gpt-oss-120b",
#     "messages": [],
#     "stream": true
#   }'
